export class OtherService {
     constructor() {
         
     }
}

//Note: this service doesn't have the @Injectable() decorator because it doesn't have any dependencies injected into its constructor